#include <iostream>
#include <cstdlib>
#include <cmath>
#include <stack>
#include <string>
#include <sstream>

using namespace std;

Term operator + (const Term& P, const Term& Q)
{
	// etc. 
	// for now: 
	return P;
}

Term operator - (const Term& P, const Term& Q)
{
	// for now: 
	return P;
}

Term operator * (const Term& P, const Term& Q)
{
	// for now: 
	return P;
}

Term operator / (const Term& P, const Term& Q)
{

}